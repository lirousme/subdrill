# Use a different value for every application hosted on this domain.
APP_SESSION_NAME=subdrill_session
# Set this to the public URL path where this repository is deployed.
APP_BASE_PATH=/subdrill
APP_ENV=production

DB_HOST=127.0.0.1
DB_NAME=subdrill
DB_USER=
DB_PASS=
